"""
ModelProvider implementations. Every vendor integration lives here behind
the same abstract interface (see base.py), so harness.py and
decision_engine.py never need to know which vendor they're calling.
"""
